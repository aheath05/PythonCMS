<IfModule mod_rewrite.c>
	RewriteEngine On
	# needs to be modified for each site
	RewriteBase /
	RewriteRule .* index.html [END]
	RewriteBase /public_html/
	RewriteRule .* index.py [END]

</IfModule>
